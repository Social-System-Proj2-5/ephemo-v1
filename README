# ブランチ命名規則

チーム開発では、以下の命名規則でブランチを作成します。

## `main`

完成品を置くブランチです。
基本的に、新しい作業ブランチは `main` から分岐させます。

## `<名前>/[実装内容]`

個人で実装を行うためのブランチです。

- `<名前>`: 実装担当者の名前
- `[実装内容]`: 実装する内容が分かる短い説明

例:

- `tanaka/login`
- `suzuki/user-profile`
- `sato/api-connect`
